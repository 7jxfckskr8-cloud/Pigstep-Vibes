# Pigstep Vibes

A standalone Minecraft music disc jukebox.

## Run locally

```bash
npm install
npm start
```

## Build a Mac app

```bash
npm run package:mac
```

The finished `.dmg` and `.zip` files are created in `dist/`.
